# Real Anthropic API key. Without it, the AI Engine always falls back to the
# deterministic Risk/Optimization Engine's own proposal (see service.py) - the service
# stays fully functional either way, per PLAN.md GD4 §4's fallback requirement.
ANTHROPIC_API_KEY=
